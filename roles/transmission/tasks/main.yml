---
- name: Ensure Media Datasets Exist
  when: inventory_hostname == groups['media'][0]
  ansible.builtin.file:
    path: "{{ item }}"
    state: directory
  loop:
    - /media/Audiobooks/
    - /media/Books/
    - /media/Comics/
    - /media/Music/
    - /media/Music Videos/
    - /media/Photos/
    - /media/Playlists/
    - /media/Podcasts/
    - /media/Videos/
    - /media/Videos/Anime TV/
    - /media/Videos/Fitness/
    - /media/Videos/Miscellaneous/
    - /media/Videos/Movies/
    - /media/Videos/TV Series/
  tags: transmission

- name: Ensure Media Download Datasets Exist
  when: inventory_hostname == groups['media'][0]
  ansible.builtin.file:
    path: "{{ item }}"
    state: directory
  loop:
    - /media/Downloads
    - /media/Downloads/incomplete
    - /media/Downloads/anime
    - /media/Downloads/audiobooks
    - /media/Downloads/books
    - /media/Downloads/comics
    - /media/Downloads/kids
    - /media/Downloads/movies
    - /media/Downloads/music
    - /media/Downloads/tv
  tags: transmission

- name: Ensure Transmission Watch Dataset Exist
  when: inventory_hostname == groups['media'][0]
  ansible.builtin.file:
    path: /media/Watch/torrentfiles
    state: directory
  tags: transmission

- name: Ensure Transmission Dataset Exist
  when: inventory_hostname == groups['media'][0]
  ansible.builtin.file:
    path: "{{ item }}"
    state: directory
  loop:
    - /datavol/apps/transmission
    - /datavol/apps/transmission/config
  tags: transmission

- name: Create Flood UI Config File
  when: inventory_hostname == groups['media'][0]
  template:
    src: config.json.j2
    dest: "{{project_root_directory}}/transmission/config.json"
  tags: transmission

- name: Deploy transmission Service
  when: inventory_hostname == groups['managers'][0]
  community.docker.docker_swarm_service:
    name: transmission
    hostname: "{{transmission_hostname}}"
    image: "{{transmission_docker_image}}:{{transmission_version}}"
    networks:
      - "{{traefik_network}}"
    cap_add:
      - NET_ADMIN
    logging:
      driver: json-file
      options:
        max-size: "10m"
    env:
      PUID: "{{ media_user }}"
      PGID: "{{ media_group }}"
      TZ: "{{ default_timezone }}"
      CREATE_TUN_DEVICE: "true"
      OPENVPN_PROVIDER: "NORDVPN"
      NORDVPN_COUNTRY: "{{ transmission_openvpn_countries }}"
      NORDVPN_CATEGORY: "P2P"
      NORDVPN_PROTOCOL: "tcp"
      OPENVPN_USERNAME: "{{ transmission_openvpn_username }}"
      OPENVPN_PASSWORD: "{{ transmission_openvpn_password }}"
      OPENVPN_OPTS: "--inactive 3600 --ping 10 --ping-exit 60"
      TRANSMISSION_SCRAPE_PAUSED_TORRENTS_ENABLED: "false"
      TRANSMISSION_WEB_UI: "flood-for-transmission"
      TRANSMISSION_WATCH_DIR_FORCE_GENERIC: "true"
      TRANSMISSION_DOWNLOAD_DIR: "/media/downloads"
      TRANSMISSION_INCOMPLETE_DIR: "/media/downloads/incomplete"
      TRANSMISSION_WATCH_DIR: "/watch"
      WEBPROXY_ENABLED: "true"
      WEBPROXY_PORT: "8118"
      LOCAL_NETWORK: "192.168.1.0/24"
      DROP_DEFAULT_ROUTE: "true"
      LOG_TO_STDOUT: "true"
      GLOBAL_APPLY_PERMISSIONS: "false"
    publish:
      - published_port: "{{ transmission_port }}"
        target_port: "{{ transmission_port }}"
        protocol: "tcp"
        mode: "host"
    mode: global
    force_update: false
    restart_config:
      condition: any
    placement:
      constraints:
        - node.id == {{docker_worker2_node_id}}
    mounts:
      - source: "{{project_root_directory}}/transmission/config/"
        target: /config
        type: bind
      - source: "{{project_root_directory}}/transmission/config.json"
        target: /opt/transmission-ui/flood-for-transmission/config.json
        type: bind
      - source: "{{project_download_directory}}/"
        target: /media/downloads
        type: bind
      - source: "{{project_watch_directory}}//torrentfiles/"
        target: /watch
        type: bind
    labels:
      traefik.http.routers.transmission.entrypoints: "websecure"
      traefik.http.routers.transmission.rule: "Host(`{{ home_tld }}`) && PathPrefix(`/transmission`)"
      traefik.http.routers.transmission.middlewares: "default-headers@file,authelia@file"
      traefik.http.routers.transmission.tls: "true"
      traefik.http.routers.transmission.service: "transmission"
      # traefik.http.routers.transmission.tls.certresolver: "{{default_certificate_resolver}}" # Un-Comment this line to use a rule-based cert
      traefik.http.services.transmission.loadbalancer.server.port: "{{ transmission_port }}"
      traefik.enable: "true"
    state: present
  tags: transmission
